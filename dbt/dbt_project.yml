name: 'lab_fundamentos_dados_g7'
version: '1.0.0'
config-version: 2

profile: 'lab_fundamentos_dados_g7'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  lab_fundamentos_dados_g7:
    marts:
      +materialized: table
      +schema: gold
